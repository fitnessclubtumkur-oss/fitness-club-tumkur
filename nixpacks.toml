# nixpacks.toml
# Explicit Nixpacks config for Railway deployment

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci --omit=dev", "npx prisma generate"]

[start]
cmd = "npx prisma db push --accept-data-loss && node src/server.js"
